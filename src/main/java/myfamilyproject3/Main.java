package myfamilyproject3;
/* public class Main {
    public static void main(String[] args) {
        // Creating humans
        Human parent1 = new Human("John");
        Human parent2 = new Human("Jane");
        Human child1 = new Human("Alice");
        Human child2 = new Human("Bob");

        // Adding children
        parent1.addChild(child1);
        parent2.addChild(child2);

        // Deleting child
        parent1.deleteChild(child1);

        // Deleting child by index
        parent2.deleteChild(0);

        // Counting family members
        System.out.println("Family size: " + parent1.countFamily());

        // Force garbage collection to delete objects
        for (int i = 10000; i <= 10000000; i += 10000) {
            // Allocate memory to fill up RAM
            byte[] memoryAlloc = new byte[i];
            // Force garbage collection
            System.gc();
        }
    }

 */